import { VenderContratoDirective } from './vender-contrato.directive';

describe('VenderContratoDirective', () => {
  it('should create an instance', () => {
    const directive = new VenderContratoDirective();
    expect(directive).toBeTruthy();
  });
});
